[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["__pycache__", ".venv", "flask_session", "instance"]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
